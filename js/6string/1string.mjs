
//anything wrapped by the double quote is string
// we can also use single quote
//single literals == backtick

//we can call variable inside the backtick(`) but it is not possible inside single quote or double quote
let name = `nikita`
let surname = `silwal`

let fullname = `my name is ${name} and surname is ${surname}`
console.log(fullname)
